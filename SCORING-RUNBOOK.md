# Perfect Racket — Scoring Change Runbook
*How to change anything in the scoring engines without breaking fittings you can't see.
Written July 2026 alongside the full algorithm audit (`algorithm-audit-2026-07-04.md` — read it first).*

## The one rule

**Never change a scoring weight, bonus, penalty, normalization bound, or DB spec without running the regression suite before and after.** The engines decide by razor margins (median #1-vs-#2 gap: 0.27 points), so a change that "obviously only affects X" will re-rank profiles you never thought about. The suite exists so you *see* every re-ranking instead of discovering it from a confused user.

## The loop

```
# 1. BEFORE touching anything — snapshot current behavior:
node test-scoring.mjs --save-baseline

# 2. Make your change in src/PerfectRacket.jsx
#    (both engines? ntrpTierAdjustment is performance-mode; computeWeights is arm-mode —
#     many concepts exist in BOTH places and must be changed twice deliberately or once deliberately)

# 3. AFTER the change:
node test-scoring.mjs path/to/latest-submissions.csv

# 4. READ THE DIFF. Every line. For each changed profile ask:
#    - Is the NEW top-3 what a good fitter would short-list for that person?
#    - Is the OLD top-3 clearly worse? (If they're equally defensible, your change is churn, not improvement.)
#    - Did anything change OUTSIDE the segment you intended? If yes, you don't understand your change yet. Stop.

# 5. Bump SCORING_VERSION in src/PerfectRacket.jsx (const near the top). Always.

# 6. Deploy, then immediately re-save the baseline:
node test-scoring.mjs --save-baseline
git add scoring-baseline.json && git commit -m "baseline after <change>"
```

## What the suite measures

- **#1 surfacing distribution** across a 1,008-profile grid (both modes × NTRP × style × swing × priority/comfort × pain) — watch for any single frame creeping past ~10% of wins (homogenization) and for the **never-in-top-3 list** growing (dead inventory; it was 13/42 pre-P1, 12/42 post).
- **NTRP≤3.0 sub-285g share** — the audit's F1 metric. Post-P1 target zone: ~20–25% on real data (the remaining light picks should be Slow & Controlled swingers and demonstrated-light current-racket players, where light is *correct*).
- **Real-submission replay** (pass a CSV) — the surfacing mix your actual traffic gets.
- **Full baseline diff** — every profile whose top-3 changed, was vs now.

## Known landmines (learned the hard way)

1. **Normalization bounds ≠ DB reality.** Several `norm(x, lo, hi)` bounds are far wider than the DB's actual spec range (head size normed 95–115; DB spans 97–100), so *documented* weights are not *effective* weights — the power score is beam-dominated in practice. Recalibrating bounds is the P2 project: it re-ranks the entire catalog and must be done as one deliberate session with before/after surfacing reports, never as a drive-by.
2. **Two engines, duplicated concepts.** Arm-health and performance paths implement maneuverability floors, tier logic, etc. separately. A fix applied to one silently leaves the other's behavior unchanged — decide *explicitly* each time whether the other engine should match.
3. **`ntrpTierAdjustment(r, ntrp, currentWeight, swingSpeed)`** has two call sites. Change the signature → change both.
4. **Specialist status lives on DB rows** (`specPower` / `specSpin` / `specControl` flags) as of P1. When refreshing the DB or renaming a model generation (Blade v9→v10 happened once already), carry the flags to the new row — the suite's surfacing report will show specialist frames vanishing if you forget.
5. **The 285g maneuverability floor** (both engines) is the structural defense against ultra-light frames winning on raw lightness. Do not remove it to "fix" a light frame not surfacing — surface it through tier logic instead.
6. **Baseline discipline:** `scoring-baseline.json` must always describe the *deployed* engine. If the diff shows changes you didn't make, someone changed code without re-saving — investigate before trusting anything.

## Version history

- **v4** (May–June 2026): performance mode shipped; maneuverability weight floor; ATP/WTA sanity validation.
- **v5.0-2026-07** (P1, this package): swing-speed-as-strength at NTRP≤3.0 (audit F1/F2); "Comfort first" comfort-weight floor of 0.24 (F7); `scoring-version` captured with every submission (F9); specialist flags moved onto DB rows (R2, behavior-neutral, verified). Evidence: real-traffic low-NTRP sub-285g #1 rate 57%→23%; zero changes at NTRP≥3.5; the canonical "muscular 2.5, fast swing" profile moves from Speed MP UL (265g) to Blackout V2 300 / Extreme MP / Boom MP (all 300g).

- **v6.0-2026-07** (P2, catalog recalibration): all 27 normalization bounds recalibrated to padded DB ranges (~15% margin — full min-max proved too aggressive, eroding P1's tier authority; padded restores balance); ≤3.0 tier points scaled ×1.5 to match the new variance; **control-density SIGN FIX** — `normInv(density)` in the control subscore had the physics inverted in BOTH engines (dense pattern scored as anti-control; the deep root of audit F5), now `norm(density)` at modest weight (perf 0.20 / arm 0.15) with head size promoted to the dominant control signal; 4.5+ heavy/dense AND-gate split (heavy earns +5 alone, dense stacks +3); maneuverability weight ×0.70 at 4.5+ (advanced players supply their own racket-head speed); low-NTRP forgiveness gate (density ≥340 −8, headSize ≤98 −5 at ≤3.0 — weight-appropriate ≠ spec-appropriate). Results: dead inventory 12→5 (Gravity Pro/Tour, Percept 97, Blade 18x20, Speed Pro/Tour all correctly alive; Gravity Pro floors at NTRP 4.0), Boom grid share 20%→7%, real margins median 0.27→0.52pts, low-NTRP sub-285 23.2% (P1 intact), canonical cases pass (5.0 S&V Control now gets Blade 18x20 > Gravity Pro — an improvement). Watch items for outcome data: Boom 14.1% real share (demand concentration, accepted), Pure Drive 13.5% (click-data flag unresolved — let outcomes decide).

- **v6.1-2026-07** (DB freshness, July 13): two catalog corrections, no weight/bound changes. (1) **Ki Q+5 DELISTED from recommendations** — the real Q+5 left Tennis Express; its slug resolved to the Ki Q+5X Pro, a different frame (27.5"/310g). Affiliate slug removed → out-of-stock convention excludes it in both engines; DB row + dropdown kept for current-racket lookups/comparisons. (2) **Ultra 100 v4 → v5** per TE spec sheet: SW 318→326, balance 6→4 pts HL, beam 26→26.5 (max convention), price $249→$299 (crosses budget brackets — display/flag only; price is in no subscore); RA 68 unchanged per TE; weight/head/pattern unchanged; specPower flag + POWER_SPECIALISTS + dropdown + affiliate key all carried. EVIDENCE: combined grid diff 263/1008 (Ki 82 · Ultra 180 · both 1), ZERO changes outside the two frames' segments; Ki was Nº1×12 (all performance; mainstream replacements), arm-health displacements all mild-pain tier with 46/49 landing on armFriendly RA 55–62 frames (3 accepted 2.5-tier slot-3 Speed MP UL backfills); Ultra ENTERED 52 boards (Power/Performance-first/Balanced — physically correct for +8 SW and wider beam), DROPPED 66 (36 Comfort-first + 6 Maneuverability — equally correct), representative margins 0.06–0.23 (razor-tie class, F3); real-app severe-pain probe: Ultra v5 scores dead-last (16.03) with injuryFactor 1.0 — the arm promise is intact. Canonical cases pass verbatim. WATCH: Speed MP L grid #1 share 12.8% (was ~11.7% pre-change — creeping past the ~10% guideline; existing trend, not introduced here; let outcome data arbitrate before tuning). SUITE BLIND SPOT FOUND (not fixed here — baseline comparability): the grid's severe tier uses "Sharp pain during play", which is NOT an app value and scores as zero pain — the suite has never exercised real severe-pain scoring; fix = its own deliberate commit + baseline re-save with Tucker's go.

- **v6.3 (Aug 10, 2026 — catalog addition + a latent-NaN fix)**: two Mantis frames enter the DB (Pro 310: 98/310g/SW330/RA67/$227; PS 300: 100/300g/SW322/RA70/$227), and `performanceSubscores` gains the `?? SETTINGS.BlankSW_Default` fallback that `computeSubscores` always had. **The NaN fix alone diffs 0/1008** (verified as its own step before the frames went in) — every current frame has a swingWeight, so the fallback is unreachable today; it is a guard against the next frame that ships without one, on the path that serves ~5 of 6 fittings. Permanent test: `npm run test:blanksw`. **The frames diff 19/1008, ALL of them arm-health + "Comfort first" + no pain, and every one is a pure insertion** (a Mantis frame enters; every other frame holds its exact relative order — machine-checked, 19/19). PS 300 takes #1 in 9 of them. **Why an RA-70 frame wins comfort-first profiles, and why it is correct-as-designed, not a bug:** `computeFrameComfort` scales the RA term by `armRisk` (`0.15 + 0.85 * armRisk`), so for a player with NO pain and no injury history the stiffness term nearly vanishes and comfort is carried by weight and balance — where the PS 300's 300g/7-pts-HL scores well. "Comfort first" with zero arm risk is a *preference*, not a *need*, and the engine correctly stops treating stiffness as decisive. The RA-66 hard gate still fires for players who actually report pain, which is why **Pro 310 never surfaces at all** (0 top-3 hits across a 1,260-profile targeted sweep on top of the 1,008 grid): RA 67 plus a 21mm control beam leaves it gated out of arm-health and out-specced on the performance path. That is expected for a control frame in a catalog this size, not a data error — worth revisiting only if Mantis's spec pipeline is ever corrected (see the WIP's trust note: the same pipeline called ~210 a swingweight). **Provenance:** swingweights are Tennisnerd measured values, deliberately chosen over the brand's own numbers, which were on a non-comparable scale. No weights, bounds, penalties, or SETTINGS changed.
- **v6.1 suite fix (July 13, harness only — NO engine change, NO version bump)**: the regression grid's severe-pain tier had used `"Sharp pain during play"`, which is NOT a key of the engine's `PAIN_NUMERIC` map, so it fell through to zero pain — **every "severe" grid row was silently scored as no-pain, and the suite had never once exercised real severe-pain scoring.** Corrected to the real max value `"Severe pain that limits play"` (and the `key()` severe-detection updated in lockstep — it keys off `.startsWith("Severe")` now, or the severe rows collide into the mild bucket). Diff was surgical: exactly the 216 severe-tier keys changed, nothing else; 1008 keys stayed unique (no collision). The change revealed the blind spot vividly — e.g. a severe-pain 2.5 Baseliner had been "getting" Ultra 100 v5 / Extreme MP (RA 66–68 stiff power frames) in the grid, and now correctly shows Clash 100L / Clash 100 (RA 54–55). **The production ENGINE was always correct here (manually verified during v6.1); only the test grid was blind.** BASELINE CAVEAT: the pre-July-13 `scoring-baseline.json` never described true severe-pain behavior — do not treat any severe-tier row in a pre-this-commit baseline as ground truth. Baseline re-saved to capture the real severe scoring. Grid `NTRP≤3.0 sub-285g` ticked 71%→73% (severe-pain low-NTRP players now correctly surface soft light frames like Clash 100L 280g — arm-appropriate, not the F1 bug); dead-inventory list unchanged at 6/42. Because scoring behavior did not change, `SCORING_VERSION` stays **v6.1-2026-07** — bumping it would falsely signal to outcome-labeling that production scoring moved.

- **v6.2-2026-07** (tension floors, July 27): **no ranking change — 0/1008 grid diff.** Fixes a live bug found in the July 27 engine deep-dive: `calcTension`'s single blanket floor of 42 let the adjustment stack (severe pain −6, low NTRP −2, slow swing −2) prescribe **natural gut at 42 lbs** — a soft, lively string strung loose, handed to exactly the hurting players the arm-health path exists to protect. Fix: **type-aware floors keyed to the string we PRESCRIBE** (`TENSION_FLOOR` = gut 48 / multi 46 / synthetic 44 / poly 42; perf mode's own 46 floor can only be raised by it), applied in BOTH engines, with the range bottom clamped to the same floor so no number below it is ever printed (floored cases show a slightly asymmetric range, e.g. "48–50, starting at 48"). Evidence: replay of 1,626 real submissions → **58 prescriptions change, every one upward, 46 natural gut + 12 multifilament, and all 58 are arm-health with reported pain**; lowest correction 42→48; 1,568 unchanged; 605 synthetic unfloored profiles verified byte-identical to the legacy formula. **KNOWN GAP, deliberately deferred (needs outcome data):** the ±2 string adjustments still key off the player's CURRENT string while the floor keys off the RECOMMENDED one — re-keying is physically correct but moves ~80% of arm-mode prescriptions (mostly +2 to +4) against the comfort-first thesis with no ground truth. Logged in the WIP. **NEW HARNESS: `npm run test:tension`** — the scoring suite diffs racket picks only and is blind to tension, which is how this survived; tension work must run it. Also noted, NOT changed: the 58 ceiling is clamped on `base` while the printed high is base+2, so the top number shown has always been 60 (mirror image of this bug; a separate decision for Tucker).

- **v6.4-2026-08 (catalog spec correction, Aug 25)**: 12 frames corrected against Tennis Express's own JSON-LD product data, re-read live rather than trusted from the audit doc. **All 15 of the audit's RA values were confirmed exactly** — its numbers were sound. **Three of its mechanism claims were not, and each is worth remembering:** (1) it says frames at RA 66 "trip the hard gate", but the gate is `ra > 66`, not `>= 66`, so Pure Strike Team / Pure Strike 100 / VCORE 100 (all →66) do NOT become gated and Radical MP / Extreme MP (both at 66) were never gated, so they were never "over-penalised"; only SX 300 (→69) starts tripping it and only TFight 315 (67) was. (2) It conflates `classicPowerFrame` with `HardGate_Penalty` — opposite mechanisms, the former a BONUS at `ra >= 66` for healthy 4.0+ perf-first players. (3) It says balance is "never scored, display-only" — balance is **20% of `computeFrameComfort`**, which BOTH engines call, and 7 of these rows had a wrong one. **The conclusion survived all three:** a wrong RA is wrong whichever gate it touches, and the exposure is broader than the doc claimed because the comfort RA term scales continuously with arm risk rather than only at a gate. **HEADLINE EVIDENCE: severe-pain boards containing a frame badged arm-friendly while TE says RA 63–66 fall from 183/216 to 24/216.** Corrected: Pure Strike Team, Speed MP L/MP/Pro 2026, Pure Strike 100, Pure Strike 100 16x20, VCORE 100 2026, SX 300 2025, Radical MP 2025, RF 01 Pro, RF 01, Gravity MP 2025 — each row now carries a `// spec verified: Tennis Express 2026-08-25` provenance comment. **3 HELD** (marked in place, Tucker's call): CX 200 16x19, Extreme MP 2025, TFight 315 — their TE pages are a Limited Edition, the 2024 model, and the "315s 2025", so the specs may describe a different physical racquet than the catalog row. Also: Pro Staff 97 v14 slug repointed (old redirected to a category page), **PERCEPT 97 slug removed** (old 404s; TE dropped the line) on the Ki Q+5 convention. **DIFF 792/1008, #1 changes in 216** (arm-health 84%, performance 67%) — large by design, every mover tracing to a corrected spec or displacement by one. **WATCH, deliberately not tuned around:** Clash 100L v3 at **22.1% of grid #1s** (was 20.4) is double the ~10% homogenization guideline and is now the biggest open scoring question; **Speed MP L collapses 66→3 #1s**, closing the standing 12.8% watch in the OPPOSITE direction from the handoff's prediction (its weight gain is swamped by RA 60→63, SW 298→312, balance 6→3); never-in-top-3 grows 7→8 (Radical MP joins; Pure Aero 2026 and EZONE 100 2025 fall to zero #1s by displacement alone). Prerequisite step, committed separately and behavior-neutral at 0/1008: **`armFriendly` is now DERIVED** from `ra <= RA_ArmFriendly_Cutoff` with commented `armFriendlyOverride` for the two editorial cases, so no row can silently claim to be arm-friendly against its own stiffness again (`npm run test:armfriendly`).

- **v6.5-2026-08 (the three held frames, Aug 25)**: completes the catalog correction. v6.4 held CX 200 16x19, Extreme MP 2025 and TFight 315 because their TE pages are the *Limited Edition*, the *2024* model and the *"315s 2025"* — a different edition or generation from the catalog row. **Tucker's call: accept them**, on the reasoning that TE sells that edition, so its specs are what players actually buy and what the affiliate link actually reaches; each row now records which TE product its numbers came from. CX 200 RA 62→65 (loses armFriendly, derived), Extreme MP 66→65 and TFight 315 67→65 (both lose the `classicPowerFrame` bonus; TFight 315 was the only one of the three genuinely tripping the RA-66 hard gate and stops). **DIFF 230/1008, #1 changes in 61**, every changed profile involving one of the three; the remainder is displacement (Gravity Tour 2025 and Speed Tour 97 2026 each gain 17 boards with no spec change). Adjudication: Extreme MP RISES 14→24 #1s because SW 323→318 outweighs the lost power bonus; CX 200 churns 41 in / 71 out for net +1 as it leaves arm-health boards; **TFight 315 is still never in a top-3 even with its hard gate removed** (SW 335→325 plus the lost bonus offset the relief) and stays dead inventory; severe-pain boards move only 6/216, since none of these three carried the arm-health promise the way the v6.4 frames did. never-in-top-3 reads 9/42, up from 8 only because PERCEPT 97 correctly joins it after the v6.4 slug removal. **The catalog's known-wrong rows are now all corrected; what remains is 22 frames never verified against anything and 20 accuracy-only deltas on unedited rows.**

## The future of tuning (from the audit)

Hand-tuned weights are v1-appropriate and defensible *because* this runbook exists. The endgame is replacing judgment with evidence: once the outcome-loop email is collecting labeled outcomes (demoed / bought / kept / arm improved), weight changes should be justified by outcome data, and the suite grows an outcomes-replay mode. Until then: every change through this loop, no exceptions, including changes suggested by an AI — *especially* those.
